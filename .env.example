# Comma-separated 2-4 default Nostr relay URLs the browser connects to.
PUBLIC_RELAY_URLS=ws://localhost:8080/ws

# LLM configuration (endpoint, model, API key) is entered in the app's
# settings — never in env. Lands with issue #11.
